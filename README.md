# auto-jsdoc-comments
Automated JSDoc block comment generator for JavaScript (and basic TypeScript). Detects functions, arrows, constants, classes, types &amp; interfaces and inserts uniform JSDoc blocks. Includes generator + degenerator scripts. Modular, clean, extensible.
